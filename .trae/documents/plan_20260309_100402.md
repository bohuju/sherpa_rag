## app.py 修改计划

### 错误修复
1. **添加缺失导入** - 在第1行添加 `FileResponse` 的导入：
   ```python
   from fastapi.responses import FileResponse
   ```

2. **修复 subprocess.run** - 第19行添加 `capture_output=True`：
   ```python
   result = subprocess.run(cmd, capture_output=True)
   ```

### 可选优化
3. 在 `index_generate` 函数中返回更详细的执行信息

修改将保持原有所有功能不变，只修复上述问题。